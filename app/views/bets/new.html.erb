<div class="new-bet">
  <%# <h1>Lance ton pari !</h1> %>
  <div class="publication-form d-flex align-items-center">

    <%= simple_form_for [ @bet ] do |f| %>
      <%# <%= f.error_messages %>


      <%= render "#{@bet.current_step}_step", :f => f %>
      <div class="publish-form-buttons d-flex justify-content-around">
        <p><%= f.submit "Précedent", class: "publish-form-button", :name => "back_button" unless @bet.first_step? %></p>
        <p><%= f.submit "Suivant", class: "publish-form-button" %></p>
      </div>
      <%# <%= f.submit "Valider", class: "invisible" unless @bet.last_step? %>
    <% end %>
  </div>
</div>
